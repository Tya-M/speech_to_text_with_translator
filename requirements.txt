# Russian Voice Translator - Dependencies
# Tested on macOS with Python 3.10+

# Core Audio
pyaudio>=0.2.14
numpy>=1.21.0,<2.0.0

# Speech Recognition - Vosk (fast, offline)
vosk>=0.3.44

# Speech Recognition - Whisper
# faster-whisper рекомендуется - 4x быстрее на CPU!
faster-whisper>=1.0.0

# Fallback: openai-whisper (медленнее, но работает без faster-whisper)
# openai-whisper>=20231117
# torch>=2.0.0

# Translation - Argos (offline)
argostranslate>=1.9.0

# Utilities
psutil>=5.9.0

# GUI
customtkinter>=5.2.0

# Optional: for building .app bundle
# py2app>=0.28.0
# pyinstaller>=6.0.0

# Vosk Large Model (опционально, ~1.5GB):
# wget https://alphacephei.com/vosk/models/vosk-model-ru-0.22.zip
# unzip vosk-model-ru-0.22.zip -d models/
